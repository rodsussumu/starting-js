const alunos = ['rodrigo', 'iago'];
alunos.push("abraao"); // Adiciona elementos no final
alunos.unshift("geovanne"); // Adiciona elementos no começo
// alunos.pop(); // Remova elementos do final
// alunos.shift(); // Remove do começo
// console.log(alunos.slice(0, 2)); Retorna de determinada distancia
console.log(alunos.slice(0, 2));